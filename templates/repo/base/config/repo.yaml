repo_profile:
  name: "{{REPO_NAME}}"
  slug: "{{REPO_SLUG}}"
  role: "{{REPO_ROLE}}"
  contract_mode: "{{CONTRACT_MODE}}"
  system_layer: "{{SYSTEM_LAYER}}"
  governing_repo: "{{GOVERNING_REPO}}"
  standards_dependency: "{{STANDARDS_REPO}}"
  standards_source_version: "{{STANDARDS_VERSION}}"
  standards_enforcement: "{{STANDARDS_ENFORCEMENT}}"
  notes: "Keep this repo aligned with {{GOVERNING_REPO}}; do not fork canonical artifacts."
