# Claude Instructions

- Stay within the role `{{REPO_ROLE}}` and respect contract mode `{{CONTRACT_MODE}}`.
- Reference canonical contracts from `{{STANDARDS_REPO}}`; do not fork them locally.
- Keep docs and playbooks concise, actionable, and aligned with `config/contracts.yaml` and `config/provenance.yaml`.
- Record open questions and next steps in context with contract and provenance updates.
- Note standards reference changes (`{{STANDARDS_VERSION}}`) in handoffs and PR descriptions.
