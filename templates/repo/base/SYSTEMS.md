# System Lifecycle (Role: {{REPO_ROLE}})

This repository is generated by system-factory with a contract-aware lifecycle. Use the canonical stage order (intake, scaffold, contracts, core-engine, review-hardening, release) and keep each stage aligned with the declared `contract_mode={{CONTRACT_MODE}}`.

Inputs and outputs should match the declarations in `config/contracts.yaml`:
- Consumed: {{CONSUMED_ARTIFACT_TYPES}}
- Produced: {{PRODUCED_ARTIFACT_TYPES}}

Standards dependency:
- Canonical source: {{STANDARDS_REPO}} @ {{STANDARDS_VERSION}}
- Supported versions: {{SUPPORTED_CONTRACT_VERSIONS}}

Keep the system file for this repo in `systems/` when you instantiate one, and ensure `required_files` include the contract declaration and provenance templates so that validation stays tied to the canonical contracts.
