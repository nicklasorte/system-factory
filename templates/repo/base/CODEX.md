# Codex Instructions

- Honor the declared role `{{REPO_ROLE}}`, system layer `{{SYSTEM_LAYER}}`, and contract mode `{{CONTRACT_MODE}}`; do not broaden scope.
- Treat `config/contracts.yaml` as the contract source; do not copy canonical schemas into this repo.
- Keep references pinned to `{{STANDARDS_REPO}} @ {{STANDARDS_VERSION}}` and the governing repo `{{GOVERNING_REPO}}`; update only with validation, provenance, and upstream coordination.
- Preserve deterministic scaffolding outputs; avoid creative rewrites that change meaning.
- Leave concise handoff notes: what changed, which contracts were touched, how governance bootstrap (`{{GOVERNANCE_BOOTSTRAP_STATUS}}`) is affected, and next validation steps.
