# Codex Instructions

- Honor the declared role `{{REPO_ROLE}}` and contract mode `{{CONTRACT_MODE}}`; do not broaden scope.
- Treat `config/contracts.yaml` as the contract source; do not copy canonical schemas into this repo.
- Keep references pinned to `{{STANDARDS_REPO}} @ {{STANDARDS_VERSION}}`; update only with validation and provenance.
- Preserve deterministic scaffolding outputs; avoid creative rewrites that change meaning.
- Leave concise handoff notes: what changed, which contracts were touched, and next validation steps.
