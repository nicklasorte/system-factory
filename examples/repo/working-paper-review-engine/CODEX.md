# Codex Instructions

- Honor the declared role `engine_repo`, system layer `Engine`, and contract mode `consume_and_produce`; do not broaden scope.
- Treat `config/contracts.yaml` as the contract source; do not copy canonical schemas into this repo.
- Keep references pinned to `nicklasorte/spectrum-systems @ main` and the governing repo `nicklasorte/spectrum-systems`; update only with validation, provenance, and upstream coordination.
- Preserve deterministic scaffolding outputs; avoid creative rewrites that change meaning.
- Leave concise handoff notes: what changed, which contracts were touched, how governance bootstrap (`enabled (SSOS control-plane assets applied)`) is affected, and next validation steps.
