# System Lifecycle (Role: engine_repo)

This repository is generated by system-factory with a contract-aware lifecycle. Use the canonical stage order (intake, scaffold, contracts, core-engine, review-hardening, release) and keep each stage aligned with the declared `contract_mode=consume_and_produce`.

Inputs and outputs should match the declarations in `config/contracts.yaml`:
- Consumed: working_paper_markdown, review_guidelines
- Produced: review_packet, risk_log

Standards dependency:
- Canonical source: spectrum-systems @ main
- Supported versions: v1

Keep the system file for this repo in `systems/` when you instantiate one, and ensure `required_files` include the contract declaration and provenance templates so that validation stays tied to the canonical contracts.
