# Claude Instructions

- Stay within the role `engine_repo` and respect contract mode `consume_and_produce`.
- Reference canonical contracts from `spectrum-systems`; do not fork them locally.
- Keep docs and playbooks concise, actionable, and aligned with `config/contracts.yaml` and `config/provenance.yaml`.
- Record open questions and next steps in context with contract and provenance updates.
- Note standards reference changes (`main`) in handoffs and PR descriptions.
